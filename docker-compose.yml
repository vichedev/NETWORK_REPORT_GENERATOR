services:
  report-generator:
    build: .
    container_name: redcom-reports-safe
    # Puerto externo 8888 -> Node non-root escuchando en 8080
    ports:
      - "8888:8080"
    volumes:
      # Base de datos, imágenes archivadas y clave de cifrado.
      # SIN este volumen se pierde todo (cuenta, empresas, historial) al recrear.
      - report_data:/app/data
    networks:
      - network_reports_bridge
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          # El backend maneja imágenes en base64 en memoria: 256M se queda corto.
          memory: 512M
    restart: always
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://localhost:8080/api/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s

volumes:
  report_data:

networks:
  network_reports_bridge:
    driver: bridge
    ipam:
      config:
        # Forzamos un pool de IPs que no choque con tus redes de ISP
        - subnet: 172.99.0.0/24
